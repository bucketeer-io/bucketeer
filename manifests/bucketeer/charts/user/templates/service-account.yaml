apiVersion: v1
kind: ServiceAccount
metadata:
    namespace: {{ .Values.namespace }}
    name: {{ template "user.name" . }}
    annotations:
{{ toYaml .Values.serviceAccount.annotations | indent 8 }}
