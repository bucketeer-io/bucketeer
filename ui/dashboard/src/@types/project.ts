export interface Project {
  createdAt: string;
  creatorEmail: string;
  description: string;
  disabled: boolean;
  id: string;
  name: string;
  organizationId: string;
  trial: boolean;
  updatedAt: string;
  urlCode: string;
}
