export interface Environment {
  archived: boolean;
  createdAt: string;
  description: string;
  id: string;
  name: string;
  organizationId: string;
  projectId: string;
  requireComment: boolean;
  updatedAt: string;
  urlCode: string;
}
