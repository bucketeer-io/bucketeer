apiVersion: v1
kind: ServiceAccount
metadata:
    namespace: {{ .Values.namespace }}
    name: {{ template "event-persister-goal-events-kafka.name" . }}
    annotations:
{{ toYaml .Values.serviceAccount.annotations | indent 8 }}
