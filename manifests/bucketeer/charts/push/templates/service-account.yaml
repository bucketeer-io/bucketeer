apiVersion: v1
kind: ServiceAccount
metadata:
    namespace: {{ .Values.namespace }}
    name: {{ template "push.name" . }}
    annotations:
{{ toYaml .Values.serviceAccount.annotations | indent 8 }}
