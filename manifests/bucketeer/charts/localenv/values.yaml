version: 0.1.0

vault:
  dev:
    enabled: true 
    devRootToken: "root"

mysql:
  initdbScriptsConfigMap: mysql-initdb
  volumePermissions:
    enabled: true
  auth:
    database: "bucketeer"
    username: "bucketeer"
    existingSecret: "mysql-secrets"
    